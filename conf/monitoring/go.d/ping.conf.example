## All available configuration options, their descriptions and default values:
## https://github.com/netdata/netdata/tree/master/src/go/collectors/go.d.plugin/modules/ping#readme

## The jobs below are generated by lib/pf/services/manager/netdata.pm.
## Only targets with a non-empty host list are emitted; Netdata v2 rejects a
## ping job whose 'hosts' is empty.
jobs:
%%ping_jobs%%
